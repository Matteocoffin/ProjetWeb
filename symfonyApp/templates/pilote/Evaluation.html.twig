{% extends 'base.html.twig'%}
{% block body %}
    <div class='text-center' id='margT3'>
        <h1>Evaluation :</h1>
    </div>
    {{ form_start(form) }}
    <div class="container" id='margT5'>
        <div>
            {{form_row(form.notesEtoiles)}}
        </div>
        <div id='margT5'>
            {{form_row(form.idEntreprise)}}
        </div>
        <div class='text-center' id='margT3'>
            <button type="submit" class="btn btn-dark">Evaluer</button>
        </div>
    </div>
    {{ form_end(form) }}

{% endblock %}